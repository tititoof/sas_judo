language: php
php:
  - '7.0'
before_script:
  - cp .env.travis .env
  - mysql -e 'create database homestead_test;'
  - composer self-update
  - composer install --no-interaction

addons:
  sonarcloud:
    organization: "sas_judo_key_for_me:master"

script:
  - vendor/bin/phpunit
  - sonar-scanner
